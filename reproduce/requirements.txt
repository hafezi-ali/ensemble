# Versions this package was verified against.
# Any recent 1.x scikit-learn reproduces the numbers; the GMM and the
# ensemble baselines are all stable API.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
